def solve_min_max():
    """
    Зчитує два натуральні числа та виводить їх мінімум, а потім максимум
    в одному рядку, розділені пробілом. (eolymp.com, задача № 2606)
    """
    try:
        # Зчитуємо два числа (a та b) з консолі, очікуючи їх в одному рядку, розділених пробілом.
        # Функція map(int, ...) перетворює кожен елемент списку на ціле число.
        a, b = map(int, input().split())
    except EOFError:
        # Обробка випадку, коли ввід завершився (актуально для автоматичних систем тестування)
        return
    except ValueError:
        # Обробка випадку некоректного вводу
        print("Помилка: Введіть два натуральні числа, розділені пробілом.")
        return

    # 1. Знаходимо мінімум та максимум за допомогою вбудованих функцій.
    minimum = min(a, b)
    maximum = max(a, b)

    # 2. Виводимо результат в одному рядку, розділений пробілом.
    print(minimum, maximum)

# Запускаємо функцію для виконання завдання
solve_min_max()
